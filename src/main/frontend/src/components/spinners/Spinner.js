import React from 'react';
import './Spinner.css';

const Spinner = () => {
    return (
        <div className="line-spinner-container">
            <div className="line-spinner"></div>
        </div>
    );
};

export default Spinner;